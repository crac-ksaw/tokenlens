apiVersion: apps/v1
kind: Deployment
metadata:
  name: tokenlens-web
spec:
  replicas: 2
  selector:
    matchLabels:
      app: tokenlens-web
  template:
    metadata:
      labels:
        app: tokenlens-web
    spec:
      containers:
        - name: web
          image: tokenlens-web:latest
          ports:
            - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: tokenlens-web
spec:
  selector:
    app: tokenlens-web
  ports:
    - port: 80
      targetPort: 80